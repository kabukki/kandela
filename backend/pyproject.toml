[project]
name = "almost"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.0",
]

[tool.fastapi]
entrypoint = "main:app"

[dependency-groups]
dev = [
    "ruff>=0.15.11",
]
